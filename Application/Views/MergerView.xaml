<UserControl x:Class="ToolKitV.Views.MergerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToolKitV.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="UNIR MÚLTIPLOS RECURSOS (MERGER)" Foreground="{StaticResource SecondaryTextBrush}" FontSize="12" Style="{StaticResource AgencyBold}" Margin="0 0 0 20"/>
            
            <local:SelectFolder x:Name="SourceRootFolder" Title="Pasta com os Sub-Recursos (veículos, packs, etc)" Margin="0 0 0 15"/>
            <local:SelectFolder x:Name="ExportFolder" Title="Pasta de Destino do Novo Pack" Margin="0 0 0 15"/>

            <Border Background="{StaticResource GlassBrush}" CornerRadius="12" Padding="20" Margin="0 10">
                <StackPanel>
                    <TextBlock Text="NOME DO NOVO RECURSO" Foreground="{StaticResource SecondaryTextBrush}" FontSize="12" Style="{StaticResource AgencyBold}" Margin="0 0 0 10"/>
                    <Border Background="{StaticResource SidebarBgBrush}" CornerRadius="8" BorderThickness="1" BorderBrush="{StaticResource LineBrush}">
                        <TextBox x:Name="MergedResourceName" Padding="10" Height="40" Background="Transparent" BorderThickness="0" Foreground="{StaticResource MainTextBrush}" Style="{StaticResource AgencyRegular}" Text="ftw_merged_pack"/>
                    </Border>
                </StackPanel>
            </Border>

            <local:Button x:Name="MergeBtn" Title="INICIAR MERGE RECURSIVO" Margin="0 20 0 0" Width="280" HorizontalAlignment="Left" Click="MergeBtn_Click"/>

            <Border x:Name="MergeProgressPanel" Visibility="Collapsed" Background="{StaticResource GlassHoverBrush}" CornerRadius="12" Padding="20" Margin="0 20 0 0" BorderBrush="{StaticResource PrimaryAccentBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock x:Name="MergeStatusTitle" Text="A PROCESSAR..." Foreground="{StaticResource PrimaryAccentBrush}" FontSize="14" Style="{StaticResource AgencyBold}" Margin="0 0 0 5"/>
                    <TextBlock x:Name="MergeStatusDesc" Text="A analisar ficheiros stream e metadados..." Foreground="{StaticResource MainTextBrush}" FontSize="12" Opacity="0.8" Style="{StaticResource AgencyRegular}"/>
                    
                    <Border Height="4" Background="{StaticResource LineBrush}" CornerRadius="2" Margin="0 15 0 0">
                        <Border x:Name="MergeProgressBar" Width="0" HorizontalAlignment="Left" Background="{StaticResource PrimaryAccentBrush}" CornerRadius="2"/>
                    </Border>
                </StackPanel>
            </Border>

            <ListView x:Name="LogList" Height="150" Background="Transparent" BorderThickness="0" Margin="0 20 0 0" Visibility="Collapsed">
                 <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" Style="{StaticResource AgencyRegular}" Opacity="0.6"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
