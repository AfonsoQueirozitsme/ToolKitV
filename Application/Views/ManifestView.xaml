<UserControl x:Class="ToolKitV.Views.ManifestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToolKitV.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="GERADOR DE MANIFESTO (FXMANIFEST)" Foreground="{StaticResource SecondaryTextBrush}" FontSize="12" Style="{StaticResource AgencyBold}" Margin="0 0 0 20"/>
            
            <local:SelectFolder x:Name="ResourceFolder" Title="Pasta do Recurso (Asset)" Margin="0 0 0 15"/>
            
            <Border Background="{StaticResource GlassBrush}" CornerRadius="12" Padding="20" Margin="0 10">
                <StackPanel>
                    <TextBlock Text="DESCRIÇÃO DO RECURSO" Foreground="{StaticResource SecondaryTextBrush}" FontSize="12" Style="{StaticResource AgencyBold}" Margin="0 0 0 10"/>
                    <Border Background="{StaticResource SidebarBgBrush}" CornerRadius="8" BorderThickness="1" BorderBrush="{StaticResource LineBrush}">
                        <TextBox x:Name="ResourceDescription" Padding="10" Height="40" Background="Transparent" BorderThickness="0" Foreground="{StaticResource MainTextBrush}" Style="{StaticResource AgencyRegular}" Text="Recurso otimizado por FTW Studios"/>
                    </Border>
                </StackPanel>
            </Border>

            <local:Button x:Name="GenerateBtn" Title="GERAR FXMANIFEST.LUA" Margin="0 20 0 0" Width="250" HorizontalAlignment="Left" Click="GenerateBtn_Click"/>
            
            <Border x:Name="ResultPanel" Visibility="Collapsed" Background="{StaticResource GlassHoverBrush}" CornerRadius="12" Padding="20" Margin="0 20 0 0" BorderBrush="{StaticResource PrimaryAccentBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="MANIFESTO GERADO COM SUCESSO!" Foreground="{StaticResource PrimaryAccentBrush}" FontSize="14" Style="{StaticResource AgencyBold}" Margin="0 0 0 5"/>
                    <TextBlock Text="O ficheiro fxmanifest.lua foi criado na pasta do recurso." Foreground="{StaticResource MainTextBrush}" FontSize="12" Opacity="0.8" Style="{StaticResource AgencyRegular}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
