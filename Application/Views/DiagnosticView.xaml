<UserControl x:Class="ToolKitV.Views.DiagnosticView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToolKitV.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="DIAGNÓSTICO DE PERFORMANCE" Foreground="{StaticResource SecondaryTextBrush}" FontSize="12" Style="{StaticResource AgencyBold}" Margin="0 0 0 20"/>
            
            <local:SelectFolder x:Name="ScanFolder" Title="Pasta para Analisar" Margin="0 0 0 15"/>
            
            <local:Button x:Name="ScanBtn" Title="INICIAR CHECK-UP" Width="200" HorizontalAlignment="Left" Click="ScanBtn_Click" Margin="0 10 0 0"/>

            <Grid x:Name="DiagnosticResult" Visibility="Collapsed" Margin="0 30 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="100" Height="100" CornerRadius="50" Background="{StaticResource SidebarBgBrush}" BorderThickness="3" BorderBrush="{StaticResource PrimaryAccentBrush}">
                    <TextBlock x:Name="ScoreLabel" Text="A" Foreground="{StaticResource PrimaryAccentBrush}" FontSize="48" Style="{StaticResource AgencyBold}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" Margin="30 0 0 0" VerticalAlignment="Center">
                    <TextBlock x:Name="DiagnosisTitle" Text="EXCELENTE PERFORMANCE" Foreground="{StaticResource MainTextBrush}" FontSize="18" Style="{StaticResource AgencyBold}"/>
                    <TextBlock x:Name="DiagnosisDesc" Text="Não foram detetados problemas graves de otimização." Foreground="{StaticResource SecondaryTextBrush}" FontSize="13" Style="{StaticResource AgencyRegular}" TextWrapping="Wrap" Margin="0 5 0 0" Opacity="0.8"/>
                </StackPanel>
            </Grid>

            <ListView x:Name="IssuesList" Background="Transparent" BorderThickness="0" Margin="0 40 0 0" Visibility="Collapsed">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource GlassBrush}" CornerRadius="8" Padding="15" Margin="0 0 0 10" BorderBrush="{StaticResource LineBrush}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" Foreground="{StaticResource PrimaryAccentBrush}" Style="{StaticResource AgencyBold}" FontSize="13"/>
                                <TextBlock Text="{Binding Description}" Foreground="{StaticResource MainTextBrush}" Opacity="0.7" FontSize="11" Margin="0 3 0 0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
