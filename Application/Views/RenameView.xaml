<UserControl x:Class="ToolKitV.Views.RenameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToolKitV.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="RENOMEAR ASSETS EM MASSA" Foreground="{StaticResource SecondaryTextBrush}" FontSize="12" Style="{StaticResource AgencyBold}" Margin="0 0 0 20"/>
            
            <local:SelectFolder x:Name="TargetFolder" Title="Pasta dos Ficheiros" Margin="0 0 0 15"/>
            
            <Border Background="{StaticResource GlassBrush}" CornerRadius="12" Padding="20" Margin="0 10">
                <StackPanel>
                    <TextBlock Text="PREFIXO PARA ADICIONAR" Foreground="{StaticResource SecondaryTextBrush}" FontSize="12" Style="{StaticResource AgencyBold}" Margin="0 0 0 10"/>
                    <Border Background="{StaticResource SidebarBgBrush}" CornerRadius="8" BorderThickness="1" BorderBrush="{StaticResource LineBrush}">
                        <TextBox x:Name="PrefixText" Padding="10" Height="40" Background="Transparent" BorderThickness="0" Foreground="{StaticResource MainTextBrush}" Style="{StaticResource AgencyRegular}" Text="ftw_"/>
                    </Border>
                    <TextBlock Text="Exemplo: carro.ytd -> ftw_carro.ytd" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" Margin="5 5 0 0" Style="{StaticResource AgencyRegular}" Opacity="0.7"/>
                </StackPanel>
            </Border>

            <local:Button x:Name="RenameBtn" Title="RENOMEAR TUDO" Margin="0 20 0 0" Width="200" HorizontalAlignment="Left" Click="RenameBtn_Click"/>

            <Border x:Name="RenameResult" Visibility="Collapsed" Background="{StaticResource GlassHoverBrush}" CornerRadius="12" Padding="20" Margin="0 20 0 0" BorderBrush="{StaticResource PrimaryAccentBrush}" BorderThickness="1">
                <TextBlock x:Name="RenameResultText" Text="0 ficheiros renomeados." Foreground="{StaticResource MainTextBrush}" FontSize="12" Style="{StaticResource AgencyRegular}"/>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
